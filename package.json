{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "test": "cross-env NODE_ENV=test jest --verbose --runInBand",
    "build:ui": "shx rm -rf build && cd ../part1/ && npm run build && shx cp -r build ../api",
    "deploy": "git push heroku master",
    "git:deploy": "git push origin master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && npm run deploy && npm run git:deploy",
    "logs:prod": "heroku logs --tail",
    "lint": "eslint ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mongoose": "^6.4.4"
  },
  "devDependencies": {
    "eslint": "^8.19.0",
    "jest": "^28.1.3",
    "nodemon": "^2.0.19",
    "shx": "^0.3.4",
    "supertest": "^6.2.4"
  }
}
